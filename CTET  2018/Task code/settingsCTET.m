%% Settings for CTET experiment

% Response keys (optional; for no subject response use empty list)
KbName('UnifyKeyNames')
escapeKey = KbName('ESCAPE');
responseKey = KbName('space');
MWresponseKey1 = KbName('1');
MWresponseKey2 = KbName('2');
MWresponseKey3 = KbName('3');
MWresponseKey4 = KbName('4');

%responseKeys = {};

% number of trials
nTrials = 50;

% How long (in seconds) each image will stay on screen
%imageDuration = 0.800;
standardDuration = 0.8;
targetDuration = 1.120;

% Background color: choose a number from 0 (black) to 255 (white)
backgroundColor = 255;

% Text color: choose a number from 0 (black) to 255 (white)
textColor = 0;

% Image format of the image files in this experiment (eg, jpg, gif, png, bmp)
imageFormat = 'bmp';

